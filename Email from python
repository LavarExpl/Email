import smtplib

server = smtplib.SMTP_SSL("smtp.gmail.com",465)
server. login("scavy20@gmail.com", "2012raceway")
server.sendmail("scavy20@gmail.com",
                'lavar.harewood@nike.com',
                "hello how are you?")

server.quit()
